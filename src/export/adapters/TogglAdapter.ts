// export/adapters/TogglAdapter.ts
// Fase 4 — Adapter Toggl (batch).
// IMPORTANTE: antes de implementar, confirmar endpoints y rate limits
// vigentes de la API de Toggl Track (pueden haber cambiado desde el
// intento anterior). Modo batch exclusivamente: throttling controlado,
// nunca una call por cada start/stop.
// TODO(Fase 4): implementar. Bloqueado hasta confirmar investigacion (seccion 3.5).
export {};
